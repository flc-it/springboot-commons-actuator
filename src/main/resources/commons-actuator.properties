# INCLUDE ALL ENDPOINTS
management.endpoints.web.exposure.include=*
# ENABLED INFO ENV
management.info.env.enabled=true

# ----------------------------------------
# COMPLEMENTARY INFO VALUES
# ----------------------------------------
info.server.environment=${server.exec.environment}
info.server.hostname=${server.exec.hostname}
info.server.port=${server.port}